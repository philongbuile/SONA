import { ConnectOptions } from './gateway';
import { Identity } from './identity/identity';
export declare const undefinedSignerMessage = "No signing implementation";
declare type SigningIdentityOptions = Pick<ConnectOptions, 'identity' | 'signer' | 'hash'>;
export declare class SigningIdentity {
    #private;
    constructor(options: Readonly<SigningIdentityOptions>);
    getIdentity(): Identity;
    getCreator(): Uint8Array;
    hash(message: Uint8Array): Uint8Array;
    sign(digest: Uint8Array): Promise<Uint8Array>;
}
export {};
//# sourceMappingURL=signingidentity.d.ts.map